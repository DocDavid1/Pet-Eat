// const express = require('express');
// const path = require('path');

// const app = express();

// // Serve static files from the 'build/server' directory
// app.use(express.static(path.join(__dirname, 'build', 'server')));

// // Serve the application for any route
// app.get('*', (req, res) => {
//     res.sendFile(path.join(__dirname, 'build', 'server', 'index.js'));
// });

// // Start the server
// const PORT = process.env.PORT || 3000;
// app.listen(PORT, () => {
//     console.log(`Server is running on port ${PORT}`);
// });
